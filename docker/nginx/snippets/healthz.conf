# Reusable health check endpoint for nginx services
# Include this file inside a server {} block.
location /healthz {
    access_log off;
    add_header Cache-Control "no-store";
    return 200 "healthy\n";
    add_header Content-Type text/plain;
}
